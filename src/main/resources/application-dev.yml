spring:
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always
  datasource:
    url: jdbc:mysql://ticketinformer.cdzzaeae8shq.ap-northeast-2.rds.amazonaws.com/ticketInformer
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ticketInformer
    password: imagineyou12!
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

    generate-ddl: true
  thymeleaf:
    #      prefix: classpath:templates/
    suffix: .html
    cache: false
  devtools:
    livereload:
      enabled=true:
